#include <stdio.h>

/**
* main - Print all single digit numbers
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int b = 0;

	while (b < 10)
	{
		printf("%d", b);
		b++;
	}
	printf("\n");
	return (0);
}
