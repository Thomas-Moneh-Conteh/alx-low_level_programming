#include "stdio.h"
#include <stdlib.h>
/**
 * _abs - returns absolute value of an int
 * @i: integer from which to get an absolute value
 * Return: Void.
 */

int _abs(int i)
{
	if (n < 0)
	{
	return (n * -1);
	}
	
	return (i);
}
