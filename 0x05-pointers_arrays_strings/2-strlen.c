#include "main.h"
#include <string.h>

/**
 * _strlen - A function that returns the length of a string.
 * @s: Length of string to be checked.
 *
 * Return: always 0;
 */

int _strlen(char *s)
{
	int len = 0;

	len = strlen(s);

	return (len);
}
